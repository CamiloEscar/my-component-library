import { customBodyConfig } from '../components/layout/CustomBody';
import { ComponentConfig } from '../utils/componentRegistry';

export const layoutPlugins: ComponentConfig[] = [
  customBodyConfig,
  // Puedes agregar más configuraciones de layout aquí en el futuro
];