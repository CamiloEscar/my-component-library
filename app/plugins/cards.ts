import { customCardConfig } from '../components/cards/CustomCard';
import { ComponentConfig } from '../utils/componentRegistry';

export const cardPlugins: ComponentConfig[] = [
  customCardConfig,
  // Puedes agregar más configuraciones de cards aquí en el futuro
];