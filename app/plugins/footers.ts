import { customFooterConfig } from '../components/footers/CustomFooter';
import { ComponentConfig } from '../utils/componentRegistry';

export const footerPlugins: ComponentConfig[] = [
  customFooterConfig,
  // Puedes agregar más configuraciones de footers aquí en el futuro
];